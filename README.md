# skinova

Project scaffolded: Vite + React + TypeScript

Scripts:

- `npm run dev` - start dev server
- `npm run build` - build production files
- `npm run preview` - preview production build

Install dependencies:

```powershell
npm install
```

Run dev server:

```powershell
npm run dev
```
